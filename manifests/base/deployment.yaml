apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: website
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: website
    spec:
      containers:
      - name: hugo
        image: hhoover/hugo:0.59.1
        imagePullPolicy: Always
        ports:
        - containerPort: 80